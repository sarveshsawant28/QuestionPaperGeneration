<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Question Paper App</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">

    
    <link rel="stylesheet" href="https://drive.google.com/uc?export=view&id=1em_jC_wbhYFjTSTEjb4B6r81GFRWdwwV">

</head>

<body class="d-flex flex-column vh-100">
    
    <% if (pattern.maxMarks === 20) { %>
        <div class="img-container">
            <img src="https://i.ibb.co/ZJ2nBn3/college-logo.png" class="img-responsive"/>
        </div>
        <table>
            <tr>
                <td>Date: <%= day %>/<%= month %>/<%= year %></td>
                <td>Internal Assesment Test: <%= type %></td>
                <td>Branch: <%= subject.branch %></td>
            </tr>
            <tr>
                <td>Semester: <%= subject.semester %></td>
                <td>Subject: <%= subject.title %></td>
                <td>Marks: 20</td>
            </tr>
        </table>    
    <% } %>
    
    <% if (pattern.maxMarks === 80) { %>
        <h5>Paper / <%= subject.title %> / <%= month %> <%= year %></h5>
        <p class="text-end">[Total Marks - <%= pattern.maxMarks %>]</p>
        <p>Notes</p>
        <ol class="semesterList">
            <li>Question <b>One</b> is <b>Compulsory</b>.</li>
            <li>Solve any <b>Three</b> out of remaining.</li>
            <li><b>Draw</b> neat and <b>clear</b> Diagrams.</li>
            <li>Assume suitable data if required</li>
        </ol>
    <% } %>

    
    <% for (let i = 0; i < questions.length; i++) {  %>
        <p class="title"><span>Q<%= i+1  %> <%= questions[i].title %></span> <span>(<%= questions[i].marks %> Marks)</span></p>
        <ol>
            <% for (let j = 0; j < questions[i].ques.length; j++) { %>
                <li> <%= questions[i].ques[j].title %></li>
            <% } %>
        </ol>
    <% } %>

    <p class="footer">********************</p>

</body>

</html>
